

    <header>

        <section class="header-section">
            <a href="{{ path('app_home') }}"><img src="{{ asset('images/Logo.png') }}" alt="Logo de Green Goodies" class="logo"></a>

            <div class="link-div">
                {% if app.user %}

                    <a href="#">Nos produits</a>
                    <a href="#">Mon panier</a>
                    <a href="#">Mon compte</a>
                    <a href="#">Déconnexion</a>

                {% else %}

                    <a href="{{path('app_login')}}">Connexion</a>
                    <a href="{{path('app_register')}}">Inscription</a>

                {% endif %}
            </div>
        </section>

    </header>

